{
  "name": "@manifestagent/agentmanifest",
  "version": "0.3.0",
  "description": "Agent Bundle spec (agent-bundle/v1) — generator, schema, validator, and examples. The Dockerfile of AI agents.",
  "type": "module",
  "bin": {
    "agentmanifest": "bin/cli.mjs",
    "agent-manifest": "bin/cli.mjs"
  },
  "files": [
    "bin",
    "schema",
    "examples",
    "README.md"
  ],
  "scripts": {
    "validate:examples": "node bin/cli.mjs validate examples/sdr && node bin/cli.mjs validate examples/scheduler"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "yaml": "^2.9.0"
  }
}
